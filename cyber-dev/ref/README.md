Drop reference screenshots here (PNG/JPG) and the visual pass gets redone against them.

The linked reference (magnific.com/free-photos-vectors/cyberpunk-ui) returns a
server-side security-filter 403 to every automated fetch, browser User-Agent
included, and the alternate design references all fail TLS verification behind
this machine's intercepting proxy. So the theme was authored from the cyberpunk
HUD idiom directly rather than from those screens.

Files here are read, not served — the harness does not load them.
